<%= form_for(@size_option) do |f| %>
  <% if @size_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@size_option.errors.count, "error") %> prohibited this size_option from being saved:</h2>

      <ul>
      <% @size_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :size %><br>
    <%= f.text_field :size %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
