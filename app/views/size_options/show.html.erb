<p id="notice"><%= notice %></p>

<p>
  <strong>Size:</strong>
  <%= @size_option.size %>
</p>

<p>
  <strong>Item:</strong>
  <%= @size_option.item_id %>
</p>

<%= link_to 'Edit', edit_size_option_path(@size_option) %> |
<%= link_to 'Back', size_options_path %>
