  <div class="col-md-6 pull-left ">
    <%= select_tag("color_option", options_from_collection_for_select(ColorOption.where(:item_id => item.id), :id, :color, 0), {class: "form-control", prompt: "Color"}) %>
    <br>
  </div>

